---
import { umamiWebsiteId, umamiScriptUrl } from '../data/config';

const enabled = umamiWebsiteId.length > 0;
---

{
  enabled && (
    <script
      is:inline
      defer
      src={umamiScriptUrl}
      data-website-id={umamiWebsiteId}
      data-domains="blueprintstudio.at,www.blueprintstudio.at"
    />
  )
}
